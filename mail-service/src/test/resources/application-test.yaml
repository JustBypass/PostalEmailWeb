spring:
  datasource:
    username: sa
    password: password
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
mail:
  account:
    email: 'nikisazikov56@gmail.com'
    appPassword: 'ffrebegf454'
fill-cache-url: 'url'
delete-cached: 'url'

